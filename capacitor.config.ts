import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.netflix.app',
  appName: 'netflix-clone-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
